Before completing a task in FrankScale, run `pnpm run format` if the environment supports it. Prefer also running `pnpm run check`, and for backend-heavy changes run `cargo test --workspace` or `pnpm run backend:check`. If dependencies or toolchains are missing, record exactly which validation steps ran, which were blocked, and why. For Vibe Kanban-linked work, keep the issue updated with concise progress notes including status, done/open items, review focus, and validation state.